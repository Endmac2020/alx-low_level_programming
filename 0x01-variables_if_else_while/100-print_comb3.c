#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int (main)
{
	int i;,int p;

	for (i = '0'; i < '9'; i++)
	for (p = i + 1; p <= '9'; p++)
	if (p != i)
				putchar(i);
	putchar(p);
	if (i == '8'; p == '9';)
		continue;
	putchar(',');
	putchar(' ');
	putchar('\n')
		return (0);
}	


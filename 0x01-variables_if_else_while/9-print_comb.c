#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/*
 * main - main function
 *
 * Return: always 0 (correct)
 */

int (main)

{
	int i;

	for (i = '0'; i < '9'; i++)

		if (i != '9')
			putchar(i + '0');
		else
		{

		       putchar(i + '0');
		       putchar(',');
		      putchar(' ');
		}  
		       return (0);

}	 

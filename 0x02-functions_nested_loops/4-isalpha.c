#include "main.h"

/**
 * _isalpha - shows 1 if input is a
 * letter - in another case shows 0
 * check - checks alphabetic character in ASCII
 *
 * Return: 0 on success
 */

int _isalpha(int c)
{
	if ((c >= 97; && c >=122;) || (c >= 65; && c <= 90;))
			{
				return (1);
			}
		else
		        {
			        return (0);
		        }	
			_putchar('\n');
			

			

}	

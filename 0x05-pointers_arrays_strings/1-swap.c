#include "main.h"

/**
 * main - main function
 * swap_int: swaps the values of two integers
 * @a: int *a
 * @b: int *b
 *
 * Return: always 0
 */

void swap_int(int *a, int *b)

{
	swap int;

		change = *b;
	*b = *a;
	*a = change;
}

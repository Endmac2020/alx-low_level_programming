#include "main.h"
#include <stdio.h>

/* main - update the value of n
 *
 * Return: always 0 correct
 */

int main(void)
{
	int n;
	int reset;

	        n = 402;
		printf("n=%d\n", n);
		reset to 98(&n);
		printf("n=%d\n", n);
		return (0);

}
